# UFO Galaxy Android - gradle.properties
# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects.
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Jetifier：关。
#
# 这一项此前是 true，上面还挂着一句 “Enable Jetpack Compose” 的注释 —— 那句话是错的。
# Jetifier 跟 Compose 毫无关系，它做的是把依赖里旧的 `android.support.*` 引用重写成
# AndroidX。本仓 android.useAndroidX=true、依赖全是 androidx / okhttp / kotlinx 这类
# 现代坐标，没有任何一个还引用 support 库，所以它一直在做无用功。
#
# 无用功本身不要紧，坏在它是**改写字节码**的一步：Jetifier 会去读每一个依赖 jar，
# 读不懂就整个构建失败。接 Robolectric 时就撞上了 ——
#
#   Failed to transform bcprov-jdk18on-1.78.1.jar ... using Jetifier.
#   Reason: IllegalArgumentException, message: Unsupported class file major version 65.
#
# major version 65 = Java 21。Robolectric 依赖的 BouncyCastle 是用 Java 21 编译的，
# 而 Jetifier 的字节码解析器不认识那个版本。注意失败发生在
# `:app:debugUnitTestCompileClasspath` 的依赖解析阶段 —— 一次挂掉五道闸
# （Protocol / Runtime / Cross-Repo / E2E / Lint），因为它们都要先编译单测。
#
# 也可以用 android.jetifier.ignorelist=bcprov 把这一个 jar 放行，但那是给一个本来
# 就不该开的开关打补丁；而且 Robolectric 依赖树里还有别的 jar，今天放行一个，
# 明天再撞一个。
#
# 想确认「到底还需不需要 Jetifier」，AGP 自带一条任务可以查：
#   ./gradlew checkJetifier
# 它会列出所有仍然引用 support 库的依赖。列表为空就说明这一项本来就该是 false。
android.enableJetifier=false
