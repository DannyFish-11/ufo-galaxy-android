<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- 包含的内容 -->
    <include domain="sharedpref" path="." />
    <include domain="database" path="." />
    <include domain="file" path="." />
    <!-- 注:cache 与 no_backup 目录由 Android 自动排除，无需(也不能)在此声明；
         之前用 domain="cache"/"no_backup" 是非法域名，触发 FullBackupContent lint 错误。 -->
</full-backup-content>
