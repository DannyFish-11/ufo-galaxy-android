<?xml version="1.0" encoding="utf-8"?>
<!-- UFO Galaxy Android - AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 权限声明 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- CRITICAL-4-FIX: SYSTEM_ALERT_WINDOW is required for FloatingIslandActivity & FloatingWindowService overlay UI. -->
    <!-- Protected at runtime by Settings > Apps > Special access > Display over other apps (user-granted). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- C10-FIX: BLUETOOTH/BLUETOOTH_ADMIN for Android <= 11; BLUETOOTH_CONNECT/SCAN for 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.NFC" />
    <!-- BUG-FIX(round2): MainActivity.requestBatteryOptimizationExemption() 发起
         ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS,缺此权限时系统静默忽略请求。 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- 硬件特性 -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <application
        android:name=".UFOGalaxyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UFOGalaxy"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">
        
        <!-- 主 Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.UFOGalaxy"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 首次启动配置引导 (PR-URL-REQUIRED) -->
        <activity
            android:name=".FirstTimeSetupActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.UFOGalaxy" />

        <!-- 悬浮窗 Activity (灵动岛) -->
        <activity
            android:name=".ui.FloatingIslandActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.UFOGalaxy.Transparent" />
        
        <!-- Galaxy 连接服务 -->
        <service
            android:name=".service.GalaxyConnectionService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="ai_assistant" />
        </service>
        
        <!-- 语音识别服务 -->
        <service
            android:name=".service.VoiceRecognitionService"
            android:enabled="true"
            android:exported="false" />
        
        <!-- 悬浮窗服务 -->
        <service
            android:name=".service.FloatingWindowService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="floating_window" />
        </service>
        
        <!-- 开机启动接收器 -->
        <!-- SECURITY-FIX-R5: exported="false" prevents any third-party app from sending explicit intents
             to trigger this receiver. BOOT_COMPLETED is a system-only protected broadcast; only the
             Android system can deliver it, so exported=false is safe and eliminates attack surface. -->
        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        
        <!-- C6-FIX: HardwareKeyReceiver exported=false to prevent arbitrary apps sending MEDIA_BUTTON -->
        <receiver
            android:name=".service.HardwareKeyReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
        
        <!-- 增强版悬浮窗服务 -->
        <service
            android:name=".service.EnhancedFloatingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="floating_ai_assistant" />
        </service>
        
        <!-- 硬件按键监听服务 (无障碍服务) -->
        <!-- CRITICAL-5-FIX: exported=false eliminates attack surface from arbitrary apps attempting to bind. -->
        <service
            android:name=".service.HardwareKeyListener"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- FileProvider for sharing the observability log file (PR15). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- 原生表面①:主屏小组件(AI 常驻桌面,对标 Friends 一体感)。 -->
        <receiver
            android:name=".widget.PresenceWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/presence_widget_info" />
        </receiver>

        <!-- 原生表面②:快捷设置磁贴(下拉即见的系统级唤醒开关)。 -->
        <service
            android:name=".tile.PresenceTileService"
            android:exported="true"
            android:icon="@drawable/ic_notification"
            android:label="@string/presence_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- BUG-FIX(round2): GitHub OAuth 回调接收 Activity 此前未在 Manifest 声明。
             OAuthManager 的 redirect_uri 为 ufo-galaxy://auth/callback,缺此声明时
             浏览器重定向无人接收,GitHub 登录流程必然 120s 超时失败。
             必须 exported=true(外部浏览器投递)+ Theme.NoDisplay(立即 finish)。 -->
        <activity
            android:name=".auth.OAuthCallbackActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="ufo-galaxy"
                    android:host="auth"
                    android:path="/callback" />
            </intent-filter>
        </activity>

        <!-- 原生表面③:全局文本入口(任意 App 选中文字→"问 Galaxy")。 -->
        <activity
            android:name=".text.ProcessTextActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay"
            android:label="@string/presence_tile_label">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- 原生表面④:默认助理角色(ROLE_ASSISTANT)。设为默认助理后,
             长按电源/侧滑手势唤起 Galaxy 而非 Gemini —— 系统级身份入口。 -->
        <service
            android:name=".assist.GalaxyVoiceInteractionService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/interaction_service" />
        </service>

        <service
            android:name=".assist.GalaxyVoiceInteractionSessionService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION" />

        <service
            android:name=".assist.GalaxyRecognitionService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
            </intent-filter>
            <meta-data
                android:name="android.speech"
                android:resource="@xml/recognition_service" />
        </service>

    </application>

</manifest>
