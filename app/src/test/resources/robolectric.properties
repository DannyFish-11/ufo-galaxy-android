# Robolectric 用哪个 SDK 级别的 android-all 跑。
#
# 不写就取 targetSdk(35)。这里显式钉在 34：android-all 是运行时从网上取的,
# 钉死一个版本可以让 CI 缓存命中稳定,也避免"某天 Robolectric 对最新 SDK 的支持
# 有回归"直接把测试打红 —— 而本仓在这一层是零覆盖起步,先要的是可靠,不是最新。
#
# minSdk 是 26；真正跟 SDK 级别有关的行为差异要单独用 @Config(sdk = [...]) 覆盖，
# 而不是靠改这里的默认值。
sdk=34
